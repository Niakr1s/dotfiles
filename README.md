# My dotfiles on OpenSuse Thumbleweed

## Bootstrapping
Run script `scripts/bootstrap.sh`.
Ensure that `$HOSTNAME` is `desktop` if you need full installation (with games, vritualization, nvidia drivers).
